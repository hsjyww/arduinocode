#include <Arduino.h>
#include <Wire.h>
#include <RTClib.h>

RTC_DS3231 rtc;

void setup()
{
    Wire.begin();
    rtc.begin();

    // 원하는 시간으로 강제 세팅 (업로드 시각으로 수정!)
    rtc.adjust(DateTime(2025, 9, 15, 15, 30, 00));

    while (1)
    {
    } // 세팅만 하고 멈춤
}

void loop() {}
